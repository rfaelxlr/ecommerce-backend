CREATE TABLE delivery_areas (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   creation_date TIMESTAMP WITHOUT TIME ZONE,
   modified_date TIMESTAMP WITHOUT TIME ZONE,
   active BOOLEAN NOT NULL,
   kilometers_distance DOUBLE PRECISION,
   price DECIMAL,
   store_id BIGINT,
   CONSTRAINT pk_delivery_areas PRIMARY KEY (id)
);

ALTER TABLE delivery_areas ADD CONSTRAINT FK_DELIVERY_AREAS_ON_STORE FOREIGN KEY (store_id) REFERENCES stores (id);